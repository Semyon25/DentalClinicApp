<Window x:Class="DentalClinicApp.Views.AddPatientDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DentalClinicApp.Views"
        mc:Ignorable="d"
        Title="Новый пациент" Height="150" Width="303" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Введите имя пациента:" Margin="0 0 0 5"/>
            <TextBox x:Name="NameTextBox" Margin="0 0 0 10"/>
        </StackPanel>
        
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Отмена" Click="Cancel_Click" Margin="0 0 10 0" Padding="3" IsCancel="True"/>
            <Button Grid.Column="2" Content="Добавить" Click="Ok_Click" Padding="3" IsDefault="True"/>
        </Grid>
    </Grid>
</Window>
